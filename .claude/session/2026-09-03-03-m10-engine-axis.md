# 2026-09-03-03 — M10 엔진 축 첫 단위: Tag/Attribute 엔진 op (자율 구간)

M6 잔여 마감(`session/2026-09-03-02-m6-remainder.md`, 커밋 `466543a`) 직후
이어서 착수. 사용자 위임 근거는 아침 회신 4번(*"quad.addTag = addTag /
quad.removeTag = removeTag / quad.setAttribute = setAttribute 를 어떤
방식으로든 채운다면, 괜찮은것 같아 … 설계/디자인 문제라기 보단, 코드 스타일
문제"*) — 방향 결정은 에이전트 몫이었고 (d′) "모든 프로바이더가 같은 형태"를
택했다(`ROADMAP.md` M10 배너의 2026-09-03 아침 회신 항목).

## 한 일

- **quad-roblox `EngineOps.luau`** — `addTag`/`removeTag`는
  `game:GetService("CollectionService")`를 **호출 시점**에 읽어(설치 시점이
  아니라) CLI spec이 `getfenv(install).game`으로 심을 수 있게(Property 핸들러
  관용구), `setAttribute`는 `inst:SetAttribute(name, v)`(nil = 삭제는 엔진
  네이티브). 셋 다 `H-238` 태깅.
- **mock** — `mockProvider`가 `installTagAttributeOps(quad)`를 부른다(한
  프로바이더가 전부). 로그 관측은 같은 함수를 `log`와 함께 **재설치**하는
  opt-in으로 유지(spec.tag/spec.attribute의 배치·깜빡임 단언이 그 로그를
  본다). mock Instance에 `SetAttribute`/`GetAttribute`/`GetAttributes`,
  `gameShim`에 CollectionService 심 — mock 경로와 실물 EngineOps 경로가
  **같은 저장소**(`instTags`/`instAttributes`)를 보도록 선언을 위로 올림.
- **스텁 안내문**(Tag.luau/AttributeKey.luau) — "quad-roblox does this in M10;
  tests use mock.installTagAttributeOps" → "install a provider with
  quad:UseProvider — a bare Quad.New() has none".
- **spec** — 신규 `quad-roblox/test/spec.tagattribute.luau`(4절: 설치·Tag
  diff·Attribute 단일/그룹/반응형/nil·생성자 blame). `spec.robloxfactory` 6은
  "M5에선 스텁"에서 "설치됨(스텁 아님)"으로 재편. `spec.tag` 2·`spec.attribute`
  8의 스텁 검증은 `installLifetime`이 이제 전체 프로바이더라 "생명주기만
  설치"가 불가 → 스텁을 **직접 호출**로(메시지·blame 단언 동일).
- **quad-types** — `Quad`에 `addTag`/`removeTag`/`setAttribute` 필드(strict
  spec이 `q.addTag` 직접 호출을 하므로 타입에 있어야 한다).
- **Studio 실측** 7/7 — `audit/m10-engine-axis-studio-2026-09-03.md`.
- **문서** — architecture EngineOps 줄의 M5/M10 분할 문장 닫음, ROADMAP M10
  배너 잔여 갱신(Event/OnChange·InstanceShorthand만 남음) + Q6 체크박스,
  round16 `H10-10`.

## 시행착오

- 첫 실행에서 넷이 깨졌다: (1)(2) spec.tag 2/spec.attribute 8 — `installLifetime`
  경유가 이제 tag op까지 심어 스텁 error가 안 남 → 직접 호출로. (3)
  spec.robloxfactory 6 — "M5 스텁" 단언이 뒤집힘(의도된 변화). (4) 새 spec의
  bare 모듈 검증이 `drive` 경유라 `bindLifetime` 스텁이 먼저 터짐 → 직접
  호출. 이어서 strict 타입 에러(`Quad`에 op 필드 없음)와 실물 생명주기
  계약(미claim 인스턴스 `drive` 거부 → `q.nativeClaim(inst)`).

## 끝 절차

- **감사 1라운드(diff 범위)** — 확실 0, 의심 1(todos/project-context가 M10
  잔여 항목을 이름까지 재나열 — 단일 소스 규약) → 포인터로 축소. 수렴
  라운드 추가 없음(diff 작음, 발견이 규약성 하나).
- **`/code-review medium`** — 4건 전부 mock 테스트 인프라(프로덕션 결함 0):
  저장소가 프록시 키라 GC 뒤 재생성된 프록시에서 태그/속성 증발(재현) →
  Data 레코드 키로 / 세 벌의 중복 뮤테이터·리더 → 비공개 헬퍼 한 벌에
  위임 / 불필요한 전방 선언 제거 / 스텁 spec이 디스패치 경유를 잃음 →
  모듈 자신의 스텁을 붙잡아 되돌리는 방식으로 복원. round16 `H10-10` 행에
  기록.
- **탐사자 생략** — 세 op 본문이 각 1~3줄이고 CLI 심·Studio 실물 양쪽 실측이
  같은 단언을 했다. 생략은 여기 기록한다(단위 끝 절차의 의도적 축소).
