# 2026-09-04-02 — M7 단위 ②: flatten + `ProcessedModifierHandler` + `Dispatch.drive` 봉합 (자율 구간)

단위 ①(`4174aa3`) 직후 착수. 정본 "flatten의 정확한 형태" 절(의사코드 둘)의
1:1 전사 + round17 §0 Q4 (a)(사용자 확정: flatten은 `Dispatch.drive`가 소유).

## 한 일

- `Modifier.luau`: `flatten`(역순 `for`, `~= nil` 건너뛰기, in-place, 슬롯을
  `ProcessedModifier`로 소진) + `ProcessedModifier`(frozen·`__tostring`,
  최상위 재노출 없음 — 정본 "공개 표면 위치" 항목).
- `Dispatch/Modifier.luau`(신설): `ProcessedModifierHandler` — `H-35` 의사코드
  그대로(HIGH, `v == ProcessedModifier`, offsetSource None → length 0 → `Void`).
  `Dispatch/init.luau`가 None 쌍 옆에서 등록하고, **`drive` 첫 줄이
  `flatten(flattened)`** — 배치 판정(`flattened[1] ~= nil`) 앞. 배열 길이를
  바꾸지 않으므로 순서 무관.
- 생성기(`gen-d.py`): `New`의 항등 `flatten` 스텁과 ③ 호출 제거 → `drive(inst,
  props)`. `Claim.luau` 헤더의 "flatten은 D 내부라 seam 미정" 불릿 → 닫힘.
- 실수 하나: require 경로 — `init.luau`에서 `./`는 상위 디렉토리 기준이라
  `../Modifier`가 한 단계 더 올라갔고, 일반 파일의 `@self/None`은 자기 자식을
  가리킨다. 둘 다 즉시 정정(스모크가 잡음).
- spec: `spec.flatten` 3절(순수 flatten 규칙 셋·drive 경유 부기/해시 병합/
  State/None·핸들러 등록/Void 철거), `spec.handlers` 10절(D.Frame), `spec.claim`
  8절(Claim 경로). Studio 3/3(`audit/m7-unit2-studio-2026-09-04.md`).
- 정본 정정: bind-system 파이프라인 의사코드 ③④(단위 ①의 ⚠️ 포인터를 실제
  갱신으로), modifier-plan flatten 절 "[구현됨]" 배너, architecture
  `Dispatch/Modifier.luau` 줄, ROADMAP 체크박스 둘, round17 `H-311`.

## 관측

`mod:X(None)` 필드는 실물에서 프로퍼티 쓰기를 건너뛴다(NoneHandler → nil →
Property `v == nil` 방어) — 정본 2-1 "실제 지우기는 NoneHandler가 담당"의 실물
귀결이고 결함이 아니다(round17 `H-311` 상태 열).

## 끝 절차

- **감사 1라운드** 확실 2(architecture `Modifier.luau` 줄의 "단위 ②는 미래" 잔존 /
  CLAUDE.md·project-context 머리말 "단위 ① 완료") + 의심 1(claim-plan `H-303`
  괄호의 미래형) — 전부 반영.
- **`/code-review medium` 1회** — 2건: 해시 키 자리의 Modifier가 소진되지 않는
  사각 → §4 `H-312`(권고 (a) flatten이 해시부도 검사해 error) / `setLength`
  4번째 인자 제거 제안은 정본 `H-35`·Tag 관용구와 맞아 기각(기록). 반박: setter
  `error(msg, 2)`, `Modifier(...)` 인자(H-310 기등록), in-place flatten, 숫자
  필드 키, `_FieldsKey` export.
- 탐사자 생략. doc-check ERROR 0, test.sh 37 파일.
